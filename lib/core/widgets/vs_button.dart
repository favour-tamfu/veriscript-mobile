import 'package:flutter/material.dart';

class VsButton extends StatelessWidget {
  const VsButton({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement
    return const SizedBox.shrink();
  }
}
